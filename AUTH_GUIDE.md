# Telegram Authentication Guide

## ✅ Исправления внедрены

Система авторизации полностью переработана для автоматического входа без дополнительных действий пользователя.

## Как это работает

### 1. **Автоматическая инициализация**

При загрузке приложения:

```typescript
// Автоматически определяет среду
- В Telegram Mini App → загружает данные пользователя
- В браузере → создает demo пользователя
```

**Логи в консоли:**
```
[TelegramAuth] Telegram WebApp initialized successfully
[TelegramAuth] User loaded from Telegram: {id: 12345, ...}
[App] Starting authentication check...
```

### 2. **Бесшовная регистрация**

**Новый пользователь:**
```
1. Открывает Mini App
2. Система автоматически:
   ✓ Создает аккаунт в БД
   ✓ Инициализирует баланс (100 TON welcome bonus)
   ✓ Генерирует JWT токен
   ✓ Сохраняет сессию (7 дней)
3. Переходит в главное меню
```

**Существующий пользователь:**
```
1. Открывает Mini App
2. Система автоматически:
   ✓ Находит пользователя в БД
   ✓ Обновляет last_login
   ✓ Загружает баланс и инвентарь
3. Переходит в главное меню
```

### 3. **Обработка ошибок**

Если что-то пошло не так:
- Показывается кнопка "Try Again"
- Подробные логи в консоли
- Пользователь может повторить попытку

## Тестирование

### В браузере (Demo режим)

```bash
npm run dev
```

Откройте `http://localhost:5173`

**Ожидаемое поведение:**
1. 1-2 секунды загрузка
2. Автоматический вход как Demo User
3. Баланс: 100 TON
4. Главное меню отображается

**Логи в консоли:**
```
[TelegramAuth] Telegram WebApp not available - running in browser mode
[TelegramAuth] Demo user created: {id: 123456789, firstName: 'Demo', ...}
[App] Authentication successful!
```

### В Telegram Mini App

1. **Создайте бота** через @BotFather
2. **Настройте Mini App:**
   ```
   /newapp
   Выберите бота
   Введите название приложения
   Введите описание
   Загрузите иконку (512x512)
   Введите URL: https://your-deployment-url.com
   ```

3. **Откройте Mini App** в Telegram

**Ожидаемое поведение:**
1. Загрузка экран (0.5-1 сек)
2. Автоматический вход
3. Профиль с вашими Telegram данными
4. Главное меню

**Логи в Telegram:**
```
[TelegramAuth] Telegram WebApp initialized successfully
[TelegramAuth] User loaded from Telegram: {id: YOUR_ID, firstName: 'Your Name', ...}
[TelegramAuth] Authenticating user: YOUR_ID
[TelegramAuth] Existing user found / New user created
[App] Authentication successful!
```

## Что исправлено

### ❌ Было:
- Пользователь видел экран приветствия
- Нужно было нажимать "Continue with Telegram"
- Не было автоматической регистрации
- Могли быть ошибки с данными Telegram

### ✅ Стало:
- **Автоматическая инициализация** при открытии
- **Мгновенная регистрация** новых пользователей
- **Welcome bonus** 100 TON для новых пользователей
- **Demo режим** для тестирования в браузере
- **Подробные логи** для отладки
- **Retry механизм** при ошибках
- **Сохранение сессии** на 7 дней

## Технические детали

### Frontend (`src/utils/telegramAuth.ts`)

```typescript
✓ Автоматическая инициализация Telegram WebApp
✓ Создание demo пользователя в браузере
✓ Получение данных через initDataUnsafe
✓ Сохранение сессии в localStorage + CloudStorage
✓ Подробные логи на каждом шаге
```

### Backend (`supabase/functions/telegram-auth/index.ts`)

```typescript
✓ Find-or-create паттерн для пользователей
✓ Автоматическое создание баланса
✓ Welcome bonus (100 TON)
✓ JWT генерация
✓ Error handling и логирование
```

### Database

```sql
✓ users - профили Telegram пользователей
✓ user_balances - балансы с welcome bonus
✓ Автоматическое создание при первом входе
```

## Отладка

### Проблема: Не авторизуется в Telegram

**Проверьте:**
1. Открыта консоль браузера
2. Есть логи `[TelegramAuth]`
3. initDataUnsafe содержит данные пользователя

**Решение:**
- Убедитесь что открыли через Telegram (не прямая ссылка)
- Проверьте что Mini App правильно настроен в BotFather
- Telegram WebApp доступен только в официальных клиентах

### Проблема: Зависание на экране загрузки

**Проверьте консоль:**
```javascript
// Ищите ошибки в логах
[App] Authentication check error: ...
[TelegramAuth] Error: ...
```

**Решение:**
- Проверьте .env файл (VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY)
- Убедитесь что Edge Functions развернуты
- Проверьте что таблицы созданы в Supabase

### Проблема: Ошибка в Edge Function

**Проверьте логи Supabase:**
1. Откройте Supabase Dashboard
2. Edge Functions → telegram-auth → Logs
3. Найдите ошибки

**Типичные проблемы:**
- Таблица users не существует
- Нет прав на запись
- Неверный формат данных

## Консольные команды для отладки

```javascript
// В консоли браузера

// Проверить текущего пользователя
telegramAuth.getCurrentUser()

// Проверить доступность Telegram WebApp
telegramAuth.isAvailable()

// Получить сохраненную сессию
telegramAuth.loadSession()

// Очистить сессию
telegramAuth.clearSession()

// Проверить Telegram данные
window.Telegram?.WebApp?.initDataUnsafe
```

## Контрольный список

- [ ] Проект собирается без ошибок (`npm run build`)
- [ ] В браузере работает demo режим
- [ ] В Telegram загружаются реальные данные пользователя
- [ ] Новый пользователь получает 100 TON
- [ ] Существующий пользователь видит свой баланс
- [ ] Сессия сохраняется между перезаходами
- [ ] При ошибке показывается кнопка "Try Again"

## Готово! 🎉

Авторизация полностью автоматическая:
- Открыл Mini App → вошел
- Новый пользователь → зарегистрирован автоматически
- Есть баланс → можно играть сразу

Никаких форм, кнопок входа или подтверждений!
